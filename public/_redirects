# Legacy sitemap paths -> the single canonical sitemap.
# No trailing-slash rules here: Cloudflare Pages already 308s a bare directory
# URL to its /-form, and a hand-written 301 in the other direction loops with it.
/sitemap.xml /sitemap-index.xml 301
/sitemap-0.xml /sitemap-index.xml 301
